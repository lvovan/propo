{
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix frontend"
  },
  "dependencies": {
    "axe-core": "^4.11.1",
    "victory": "^37.3.6"
  }
}
